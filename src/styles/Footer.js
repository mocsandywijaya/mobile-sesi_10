import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  footer: {
    backgroundColor: '#333',
    color: '#fff', // Di React Native, warna teks diatur menggunakan properti `color` dalam elemen seperti Text
    textAlign: 'center', // React Native menggunakan `textAlign`
    padding: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },
});

export default styles;
